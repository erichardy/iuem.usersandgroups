jQuery(document).ready(function($) {

/*
    $("a.overlay").prepOverlay({
            subtype: 'ajax',
            filter: '#content > *',
            formselector: 'form',
            config: {expose:{color:'#00f'}},
        });
*/
});